{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Quick Note's local windows.",
  "windows": ["capture", "history"],
  "permissions": ["core:default"]
}
